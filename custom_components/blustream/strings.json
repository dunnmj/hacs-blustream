{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "[%key:common::config_flow::data::host%]",
          "port": "[%key:common::config_flow::data::port%]",
          "name": "[%key:common::config_flow::data::name%]",
          "power_on_app_source_change": "Power on Matrix if source is changed on phone app?"
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    }
  },
  "services": {
    "send_output_guest_command": {
      "name": "Send Output Guest Command",
      "description": "Sends a command string to the serial port of the device that the selected media player entity is part of.",
      "fields": {
        "command_string_bytes": {
          "description": "Command string to send as a list of decimal encoded bytes. For ACM200/210 you may have to specify the same command more than once.",
          "name": "Command"
        }
      }
    },
    "send_input_guest_command": {
      "name": "Send Input Guest Command",
      "description": "Sends a command string to the serial port of the device that the selected camera entity is part of.",
      "fields": {
        "command_string_bytes": {
          "description": "Command string to send as a list of decimal encoded bytes. For ACM200/210 you may have to specify the same command more than once.",
          "name": "Command"
        }
      }
    }
  }
}
